"""Pure domain logic (value objects, invariants) shared across services.

Populated as needed starting Phase 3. Deliberately framework-free: no FastAPI,
SQLAlchemy, or LangChain/LangGraph imports belong here.
"""
